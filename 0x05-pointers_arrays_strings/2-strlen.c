#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: the string to be counted
 *
 * Return: length of string s
 */
int _strlen(char *s)
{
	int size = sizeof(s);

	return (size - 1);
}
