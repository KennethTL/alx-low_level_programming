#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: first decimal parameter
 * @b: second decimal parameter
 *
 * Return: Sum of a and b
 */
int add(int a, int b)
{
	int rval = a + b;

	return (rval);
}
